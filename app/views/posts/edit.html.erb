<h1>Posts#edit</h1>
<p>Find me in app/views/posts/edit.html.erb</p>


<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        投稿編集
      </h1>
    </div>
  </div>
</section>

<% if @post.errors.any? %>
  <div class="notification is-danger">
    <h2><%= pluralize(@post.errors.count, "error") %> prohibited this object from being saved: not successfully</h2>
    <ul>
      <% @post.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-6">



        <%= form_for @post do |f| %>
          <div class="field">
            <%= f.label :title, "SPOT", class: "label" %>
            <%= f.text_field :title, class: "input" %>
          </div>

          <div class="field">
            <%= f.label :body, "内容", class: "label" %>
            <%= f.text_area :body, class: "textarea" %>
          </div>


          <%= f.submit '投稿', class: "button is-success" %>




        <% end %>


                  <% @post.post_images.each do |img| %>
                    

                      <%= attachment_image_tag img, :post_image, fallback: "noimage.png", size: '400x320' %>

                      <%= form_for(img,url:post_post_image_path(@post,img), method: :patch) do |f| %>

                        

                        <div class="field">
                      

                      <%= f.attachment_field :post_image, class: "input" %>
                        </div>

                        <%= f.submit '写真の変更', class: "button is-success" %>

                      <% end %>

                      


                  <% end %>


          <br>
          </br>

<br>
          </br>
<br>
          </br>

<br>
          </br>

          <br>
          </br>


            
      </div>
    </div>
  </div>
</section>
